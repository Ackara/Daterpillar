CREATE DATABASE [GenerateTSqlSchemaWithSettingsDisabled];
USE [GenerateTSqlSchemaWithSettingsDisabled];

CREATE TABLE [Employee]
(
	[Id] int NOT NULL IDENTITY(1, 1),
	[Full_Name] int not null default 'n/a',
	[Salary] int not null,
	PRIMARY KEY ([Id] ASC),
	FOREIGN KEY ([Id]) REFERENCES [Card] ([Id]) ON UPDATE SET NULL ON DELETE CASCADE
);

SET IDENTITY_INSERT [Employee] ON;
CREATE UNIQUE INDEX employee_idx ON [Employee] ([Full_Name] DESC);

CREATE TABLE [Manager]
(
	[Id] int NOT NULL IDENTITY(1, 1),
	[Full_Name] int not null default 'n/a',
	[Salary] int not null,
	PRIMARY KEY ([Id] ASC),
	FOREIGN KEY ([Id]) REFERENCES [Card] ([Id]) ON UPDATE SET NULL ON DELETE CASCADE
);

SET IDENTITY_INSERT [Manager] ON;
CREATE UNIQUE INDEX manager_idx ON [Manager] ([Full_Name] DESC);

-- Script Goes Here